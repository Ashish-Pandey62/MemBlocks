---
phase: 09-metrics-reporting
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - evaluation/runners/locomo.py
autonomous: true
requirements:
  - METR-01
  - METR-02
  - METR-03
must_haves:
  truths:
    - Runner evaluates generated answers using the LocomoEvaluator.
    - Run results aggregate accuracy globally and by reasoning type.
    - Token tracking is collected for every evaluation across stages.
  artifacts:
    - path: "evaluation/runners/locomo.py"
      provides: "Aggregated stats dictionary"
  key_links:
    - from: "evaluation/runners/locomo.py"
      to: "evaluation/metrics/locomo.py"
      via: "calls LocomoEvaluator.evaluate_answer"
---

<objective>
Integrate the LLM Judge evaluation into the Locomo runner loop, calculate accuracy statistics, and collect stage-based token metrics.

Purpose: To process the raw results of the pipeline into summarized accuracy formats (METR-01, METR-02) and record token tracking (METR-03).
Output: Modified `LocomoRunner` that augments its raw results with evaluation scores and produces an aggregated metrics dictionary.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-metrics-reporting/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integrate LocomoEvaluator</name>
  <files>evaluation/runners/locomo.py</files>
  <action>
    Import `LocomoEvaluator` from `evaluation.metrics.locomo`.
    In `LocomoRunner._run_async`, after calling `self._call_llm()` to get `answer_hybrid` (or whichever strategy answer is generated), invoke `LocomoEvaluator().evaluate_answer(...)`.
    Assign the "Pass" or "Fail" score to `eval_result["score_hybrid"]` (or similar key for the specific strategy being graded).
    Capture the LLM prompt and response trace of the QA evaluation and add them to the `eval_result` dictionary for debugging and qualitative review (e.g. `eval_result["prompt_trace"]`, `eval_result["response_trace"]`).
    Add stub token metrics to `eval_result["tokens"]` simulating the breakdown (`retrieval`, `extraction`, `qa`, `judge`, etc.) required by the stage-based tracking.
  </action>
  <verify>
    <automated>python -c "from evaluation.runners.locomo import LocomoRunner; import inspect; assert 'LocomoEvaluator' in inspect.getsource(LocomoRunner._run_async)"</automated>
  </verify>
  <done>Evaluations contain pass/fail scores and nested token usage dicts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Aggregate Stats by Reasoning Type</name>
  <files>evaluation/runners/locomo.py</files>
  <action>
    At the end of `LocomoRunner._run_async`, after the sessions loop, calculate aggregate statistics.
    1. Overall Accuracy: total Passes / total questions evaluated.
    2. Accuracy by Reasoning Type: group questions by `question.category` and calculate accuracy for each category.
    3. Aggregate Tokens: sum all stage tokens across the run.
    Return these calculated metrics inside the runner's final dictionary result (e.g., `{"sessions_processed": N, "details": results, "metrics": {...}}`).
  </action>
  <verify>
    <automated>python -c "from evaluation.runners.locomo import LocomoRunner; import inspect; src = inspect.getsource(LocomoRunner._run_async); assert 'metrics' in src or 'aggregate' in src"</automated>
  </verify>
  <done>LocomoRunner output dictionary contains a 'metrics' object with global and category accuracies.</done>
</task>

</tasks>

<verification>
Review `evaluation/runners/locomo.py` to ensure stats aggregation covers reasoning types and the judge model grading loop works per question.
</verification>

<success_criteria>
`LocomoRunner` outputs comprehensive metrics alongside raw details.
</success_criteria>

<output>
After completion, create `.planning/phases/09-metrics-reporting/09-02-SUMMARY.md`
</output>