---
phase: 09-metrics-reporting
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - evaluation/runners/locomo.py
  - evaluation/metrics/reporter.py
autonomous: true
requirements:
  - METR-04
must_haves:
  truths:
    - After an evaluation run, a JSON report, a CSV report, and run_info.json are saved in the output directory.
    - Console outputs a summary table grouping metrics by reasoning type.
  artifacts:
    - path: "evaluation/metrics/reporter.py"
      provides: "Reporter class with save_json, save_csv, print_summary methods"
  key_links:
    - from: "evaluation/runners/locomo.py"
      to: "evaluation/metrics/reporter.py"
      via: "instantiates Reporter at end of run"
---

<objective>
Generate JSON, CSV, and console output reports for analysis and reproducibility.

Purpose: To ensure the metrics calculated in the evaluation pipeline are exportable and human-readable (METR-04).
Output: A Reporter module that serializes the runner's dictionary results and prints formatted summaries.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-metrics-reporting/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Reporter Exporters</name>
  <files>evaluation/metrics/reporter.py</files>
  <behavior>
    - save_json saves the full nested dict.
    - save_csv flattens the details list into row-per-question format.
  </behavior>
  <action>
    Create `evaluation/metrics/reporter.py`.
    Implement `class Reporter`.
    - `save_json(results: dict, output_dir: Path, filename="report.json")`
    - `save_csv(results: dict, output_dir: Path, filename="report.csv")`: Iterates over `results["details"]` -> `session["evaluations"]` and writes one row per question. Include session_id, question, expected_answer, actual_answer, score, category.
    - `save_run_info(config: EvalConfig, output_dir: Path, filename="run_info.json")`: serializes the Pydantic config model to JSON.
  </action>
  <verify>
    <automated>python -c "from evaluation.metrics.reporter import Reporter; assert hasattr(Reporter, 'save_csv')"</automated>
  </verify>
  <done>Reporter class has all export methods.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Console Summary and Integration</name>
  <files>evaluation/runners/locomo.py, evaluation/metrics/reporter.py</files>
  <action>
    1. Add a `print_summary(results: dict)` method to `Reporter`. It should format the `results["metrics"]` (Overall Accuracy, By Reasoning Type, and Token Summary) and print it to standard output.
    2. Modify `LocomoRunner.run` (or the top-level executor of the runner) to instantiate `Reporter` and call its `save_json`, `save_csv`, `save_run_info` (passing `self.config`), and `print_summary` methods before returning the final dictionary.
  </action>
  <verify>
    <automated>python -c "from evaluation.metrics.reporter import Reporter; assert hasattr(Reporter, 'print_summary')"</automated>
  </verify>
  <done>Runner delegates to Reporter at the end of the run and prints a formatted console summary.</done>
</task>

</tasks>

<verification>
Check `evaluation/runners/locomo.py` to confirm that all reports are outputted to the `output_dir` provided during `run()`.
</verification>

<success_criteria>
Evaluating a dataset yields `report.json`, `report.csv`, and `run_info.json` in the run's folder alongside a terminal summary.
</success_criteria>

<output>
After completion, create `.planning/phases/09-metrics-reporting/09-03-SUMMARY.md`
</output>