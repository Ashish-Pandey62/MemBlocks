---
phase: 09-metrics-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evaluation/core/config.py
  - evaluation/metrics/locomo.py
autonomous: true
requirements:
  - METR-01
  - METR-03
must_haves:
  truths:
    - User can specify the judge model in the runner configuration.
    - System can evaluate an LLM answer as Strictly Pass or Fail without CoT.
    - System defines data structures for stage-based token usage tracking.
  artifacts:
    - path: "evaluation/core/config.py"
      provides: "judge_model configuration"
    - path: "evaluation/metrics/locomo.py"
      provides: "LocomoEvaluator and TokenTracker structures"
  key_links:
    - from: "evaluation/metrics/locomo.py"
      to: "evaluation/core/config.py"
      via: "imports RunnerConfig"
---

<objective>
Implement the LLM-as-a-Judge evaluation logic for pass/fail accuracy scoring and setup configuration and token tracking structures.

Purpose: To calculate the quality of responses accurately (METR-01) and prepare for structured token tracking (METR-03) without coupling metrics directly into the runner's execution logic.
Output: LocomoEvaluator class with strict Pass/Fail judging, and data structures for granular token tracking.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From evaluation/core/config.py:
```python
class RunnerConfig(BaseModel):
    name: str = Field(..., description="Name of the runner")
    model: Optional[str] = Field(None, description="Model to use")
    # Needs judge_model
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Configuration Models</name>
  <files>evaluation/core/config.py</files>
  <behavior>
    - RunnerConfig parses `judge_model` from YAML correctly (defaulting to None if omitted).
  </behavior>
  <action>
    Add `judge_model: Optional[str] = Field(None, description="Model to use for LLM judge")` to `RunnerConfig`.
  </action>
  <verify>
    <automated>python -c "from evaluation.core.config import RunnerConfig; c=RunnerConfig(name='x', judge_model='gpt-4'); assert c.judge_model == 'gpt-4'"</automated>
  </verify>
  <done>RunnerConfig supports judge_model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LocomoEvaluator & Token Tracking Structs</name>
  <files>evaluation/metrics/locomo.py</files>
  <behavior>
    - `LocomoEvaluator.evaluate` returns "Pass" or "Fail" based on LLM response mock.
    - Token structures store usage per pipeline stage (retrieval, extraction, qa, etc.).
  </behavior>
  <action>
    Create `evaluation/metrics/locomo.py`.
    1. Import `RunnerConfig` from `evaluation.core.config`.
    2. Define `StageTokenUsage` (BaseModel) or a standard dictionary structure to track tokens for explicit stages: `retrieval`, `extraction` (PS1), `conflict_management` (PS2), `summary_generation`, `core_memory_generation`, `qa`, and `judge`.
    3. Create `LocomoEvaluator` class. It should take a `RunnerConfig` instance (or a `judge_model` string from it).
    4. Add a method `evaluate_answer(question: str, expected_answer: str, actual_answer: str) -> str`. It will mock an LLM call using strict prompt instructing it to return ONLY "Pass" or "Fail" without CoT. IMPORTANT: explicitly instruct the judge to grade 'Pass' if the answer is factually correct, ignoring whether it hallucinated using outside knowledge instead of retrieved context. For the stub implementation, if `actual_answer` matches or contains the `expected_answer`, return "Pass", else "Fail".
  </action>
  <verify>
    <automated>python -c "from evaluation.metrics.locomo import LocomoEvaluator; assert LocomoEvaluator().evaluate_answer('Q', 'A', 'A') == 'Pass'"</automated>
  </verify>
  <done>LocomoEvaluator correctly grades pass/fail and token structs exist.</done>
</task>

</tasks>

<verification>
Ensure config can initialize judge_model and LocomoEvaluator can simulate strict grading.
</verification>

<success_criteria>
`judge_model` exists in configuration and `LocomoEvaluator` returns Pass/Fail accurately.
</success_criteria>

<output>
After completion, create `.planning/phases/09-metrics-reporting/09-01-SUMMARY.md`
</output>