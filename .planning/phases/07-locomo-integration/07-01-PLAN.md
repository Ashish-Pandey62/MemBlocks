---
phase: 07-locomo-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evaluation/core/config.py
  - evaluation/datasets/locomo.py
  - evaluation/datasets/__init__.py
  - tests/test_locomo_dataset.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "System can load LocomoDataset using DatasetConfig."
    - "System groups raw HuggingFace dataset items into complete conversation sessions."
    - "Character names are prepended to both user and assistant message content."
    - "System accurately extracts multiple choice options, answers, and question types."
    - "System can limit the number of processed sessions and questions per session."
  artifacts:
    - path: "evaluation/datasets/locomo.py"
      provides: "LoCoMo dataset parser and structures"
      exports: ["LocomoMessage", "LocomoQuestion", "LocomoSession", "LocomoDataset"]
    - path: "tests/test_locomo_dataset.py"
      provides: "Tests for parsing and subsetting"
      contains: "def test_locomo"
  key_links:
    - from: "evaluation/datasets/locomo.py"
      to: "evaluation/core/registry.py"
      via: "get_registry().register_dataset"
      pattern: "register_dataset.*locomo"
---

<objective>
Implement the LoCoMo dataset integration for the evaluation framework.

Purpose: Accurately download and parse the `locomo-mc10` dataset from HuggingFace to evaluate MemBlocks.
Output: A new `LocomoDataset` component registered in the framework, with corresponding tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-locomo-integration/07-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
From evaluation/core/config.py:
```python
class DatasetConfig(BaseModel):
    name: str
    path: Optional[str] = None
```

From evaluation/datasets/base.py:
```python
class BaseDataset(ABC):
    def __init__(self, config: DatasetConfig) -> None:
        self.config = config

    @abstractmethod
    def load(self) -> Any:
        pass
```

From evaluation/core/registry.py:
```python
def get_registry() -> Registry:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write interface contracts and test scaffold</name>
  <files>evaluation/core/config.py, evaluation/datasets/locomo.py, evaluation/datasets/__init__.py, tests/test_locomo_dataset.py</files>
  <action>
    - Update `DatasetConfig` in `evaluation/core/config.py` to add `max_sessions: Optional[int] = None` and `max_questions_per_session: Optional[int] = None`.
    - Create `evaluation/datasets/locomo.py` with dataclasses `LocomoMessage(role: str, content: str)`, `LocomoQuestion(question: str, choices: List[str], answer_idx: int, reasoning_type: str)`, and `LocomoSession(session_id: str, messages: List[LocomoMessage], questions: List[LocomoQuestion])`.
    - Create `LocomoDataset(BaseDataset)` class in `evaluation/datasets/locomo.py` with an empty `load()` method returning `List[LocomoSession]`.
    - Register it using `get_registry().register_dataset("locomo", LocomoDataset)`.
    - Expose `LocomoDataset` in `evaluation/datasets/__init__.py`.
    - Create `tests/test_locomo_dataset.py` with an empty test verifying imports.
  </action>
  <verify>
    <automated>pytest tests/test_locomo_dataset.py</automated>
  </verify>
  <done>Interfaces defined, component registered, and test scaffold runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement LoCoMo parsing logic</name>
  <files>evaluation/datasets/locomo.py, tests/test_locomo_dataset.py</files>
  <behavior>
    - Test 1: Given mocked HuggingFace dataset items, it groups them correctly by `haystack_session`.
    - Test 2: It maps original roles to "user" and "assistant", prepending character tags (e.g. `[Caroline]: hello`).
    - Test 3: It accurately extracts 10 choices, correct answer index, and reasoning types into `LocomoQuestion` objects.
  </behavior>
  <action>
    - Implement `load()` in `LocomoDataset`.
    - Use HuggingFace `datasets.load_dataset("bdsaglam/locomo-mc10", split="test")` (or the default split).
    - Parse the raw dataset rows into a list of `LocomoSession` instances.
    - Implement role mapping and character tag prepending as requested by user context.
    - Note: Only import datasets at runtime or handle missing `datasets` dependency gracefully if it's not installed. Assume it is installed for the tests.
  </action>
  <verify>
    <automated>pytest tests/test_locomo_dataset.py</automated>
  </verify>
  <done>Data parsing strictly follows all format mapping rules from CONTEXT.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dataset subsetting</name>
  <files>evaluation/datasets/locomo.py, tests/test_locomo_dataset.py</files>
  <behavior>
    - Test 1: Loading with `max_sessions=N` returns exactly N sessions.
    - Test 2: Loading with `max_questions_per_session=M` returns exactly M questions per session.
  </behavior>
  <action>
    - Update `LocomoDataset.load()` to slice the sessions list to `max_sessions` if provided in `self.config`.
    - Update `LocomoDataset.load()` to slice each session's questions list to `max_questions_per_session` if provided.
  </action>
  <verify>
    <automated>pytest tests/test_locomo_dataset.py</automated>
  </verify>
  <done>Subsetting logic prevents memory/time explosions during full evaluation runs.</done>
</task>

</tasks>

<verification>
Check that `LocomoDataset` properly registers, parses complex dialogue structures, and applies subsetting configuration limits correctly.
</verification>

<success_criteria>
- The dataset component parses LoCoMo into sequential `LocomoSession` objects.
- HuggingFace datasets are downloaded automatically.
- All tests pass in `pytest tests/test_locomo_dataset.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-locomo-integration/07-01-SUMMARY.md`
</output>