---
phase: 06-framework-architecture
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - evaluation/datasets/base.py
  - evaluation/metrics/base.py
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - Developers can define datasets by subclassing an abstract base class.
    - Developers can define metrics by subclassing an abstract base class.
  artifacts:
    - path: evaluation/datasets/base.py
      provides: BaseDataset ABC
    - path: evaluation/metrics/base.py
      provides: BaseMetric ABC
  key_links:
    - from: evaluation/datasets/base.py
      to: evaluation/core/config.py
      via: config consumption
---

<objective>
Define the abstract base classes for Datasets and Metrics.

Purpose: Enforce a consistent interface for loading data and calculating metrics across the evaluation framework.
Output: Base module interfaces that will be extended in future phases.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-framework-architecture/06-RESEARCH.md
</context>

<interfaces>
From evaluation/core/config.py:
```python
class DatasetConfig(BaseModel):
    type: str
    path: str
    # other kwargs
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseDataset ABC</name>
  <files>evaluation/datasets/base.py, evaluation/datasets/__init__.py</files>
  <action>
    Create `evaluation/datasets/base.py` defining a `BaseDataset` ABC (Abstract Base Class). It should have an `__init__` that takes a `DatasetConfig` and an abstract `load()` method. 
    Create `evaluation/datasets/__init__.py` to make it a module.
  </action>
  <verify>
    <automated>python -c "from evaluation.datasets.base import BaseDataset"</automated>
  </verify>
  <done>BaseDataset interface is established</done>
</task>

<task type="auto">
  <name>Task 2: Create BaseMetric ABC</name>
  <files>evaluation/metrics/base.py, evaluation/metrics/__init__.py</files>
  <action>
    Create `evaluation/metrics/base.py` defining a `BaseMetric` ABC. It should have an abstract `compute(self, results: dict) -> dict` method.
    Create `evaluation/metrics/__init__.py` to make it a module.
  </action>
  <verify>
    <automated>python -c "from evaluation.metrics.base import BaseMetric"</automated>
  </verify>
  <done>BaseMetric interface is established</done>
</task>

</tasks>

<success_criteria>
- Abstract base classes for Datasets and Metrics are defined.
- Interfaces are strictly enforced via Python's abc module.
</success_criteria>

<output>
After completion, create `.planning/phases/06-framework-architecture/06-02-SUMMARY.md`
</output>