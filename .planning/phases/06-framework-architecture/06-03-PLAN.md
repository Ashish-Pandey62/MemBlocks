---
phase: 06-framework-architecture
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - evaluation/runners/base.py
  - evaluation/eval.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - Developers can define runners by subclassing an abstract base class.
    - Evaluation can be executed from a single entrypoint script with a --config flag.
    - Evaluation creates timestamped output directories for results.
    - A failure in one run does not halt subsequent runs in a multi-run config.
  artifacts:
    - path: evaluation/runners/base.py
      provides: BaseRunner ABC
    - path: evaluation/eval.py
      provides: Main entrypoint script
  key_links:
    - from: evaluation/eval.py
      to: evaluation/core/config.py
      via: config parsing
    - from: evaluation/eval.py
      to: evaluation/runners/base.py
      via: runner instantiation and execution
---

<objective>
Define the BaseRunner abstract class and wire the main evaluation entrypoint script.

Purpose: Provide a unified interface for executing evaluation pipelines and handling multi-run configurations gracefully.
Output: BaseRunner ABC and the `eval.py` entrypoint.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-framework-architecture/06-RESEARCH.md
</context>

<interfaces>
From evaluation/core/config.py:
```python
def load_config(path: Path) -> EvalConfig: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseRunner ABC</name>
  <files>evaluation/runners/base.py, evaluation/runners/__init__.py</files>
  <action>
    Create `evaluation/runners/base.py` defining a `BaseRunner` ABC. It should take a `RunnerConfig` and a `BaseDataset` instance in `__init__`, and have an abstract `run(self, output_dir: Path)` method.
    Create `evaluation/runners/__init__.py` to make it a module.
  </action>
  <verify>
    <automated>python -c "from evaluation.runners.base import BaseRunner"</automated>
  </verify>
  <done>BaseRunner interface is established</done>
</task>

<task type="auto">
  <name>Task 2: Implement eval.py entrypoint</name>
  <files>evaluation/eval.py</files>
  <action>
    Create `evaluation/eval.py` as the main entrypoint. It should:
    1. Parse a `--config` CLI argument using argparse.
    2. Load the configuration via `load_config`.
    3. Loop over `config.runs`. For each run:
       a. Create a timestamped directory `evaluation/runs/YYYYMMDD_HHMMSS_{run.name}/`.
       b. Use a try/except Exception block to execute the run gracefully.
       c. Log errors but continue to the next run if an exception occurs.
    (It won't instantiate real runners yet since they aren't implemented, just add the structural scaffolding/placeholders).
  </action>
  <verify>
    <automated>python evaluation/eval.py --help</automated>
  </verify>
  <done>eval.py provides a clear CLI interface and handles multi-run execution flow</done>
</task>

</tasks>

<success_criteria>
- The system supports single-script execution via eval.py.
- eval.py creates distinct output directories for each run.
- eval.py correctly handles exceptions without crashing the entire suite.
</success_criteria>

<output>
After completion, create `.planning/phases/06-framework-architecture/06-03-SUMMARY.md`
</output>