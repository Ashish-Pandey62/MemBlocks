---
phase: 06-framework-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - evaluation/core/config.py
  - evaluation/core/registry.py
  - evaluation/configs/example_config.yaml
autonomous: true
requirements:
  - EVAL-02
must_haves:
  truths:
    - Configuration files are parsed using Pydantic and PyYAML.
    - Multi-run configurations are supported.
  artifacts:
    - path: evaluation/core/config.py
      provides: Pydantic models for configuration
    - path: evaluation/core/registry.py
      provides: Component registry
    - path: evaluation/configs/example_config.yaml
      provides: Example configuration file
  key_links:
    - from: evaluation/core/config.py
      to: evaluation/configs/example_config.yaml
      via: config parsing
---

<objective>
Establish the core configuration models and component registry for the new evaluation framework.

Purpose: Move away from complex CLI flags by defining a clean, typed configuration system.
Output: Pydantic models for config validation and a registry for dynamic component loading.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-framework-architecture/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies</name>
  <files>pyproject.toml</files>
  <action>Run `uv add pyyaml pydantic` to add the required configuration libraries to the project.</action>
  <verify>
    <automated>uv pip list | findstr "pydantic pyyaml"</automated>
  </verify>
  <done>pyyaml and pydantic are installed and present in pyproject.toml</done>
</task>

<task type="auto">
  <name>Task 2: Create config models</name>
  <files>evaluation/core/config.py, evaluation/configs/example_config.yaml</files>
  <action>
    Create `evaluation/core/config.py` with Pydantic models `DatasetConfig`, `RunnerConfig`, `RunConfig` (has name, dataset, runner, and a list of metric names), and `EvalConfig` (contains a list of `runs`). Add a function `load_config(path: Path) -> EvalConfig` that uses PyYAML to parse a file and pass it to Pydantic.
    Create `evaluation/configs/example_config.yaml` with an example multi-run configuration.
  </action>
  <verify>
    <automated>python -c "from evaluation.core.config import load_config; from pathlib import Path; load_config(Path('evaluation/configs/example_config.yaml'))"</automated>
  </verify>
  <done>Config models successfully parse the example YAML file</done>
</task>

<task type="auto">
  <name>Task 3: Create component registry</name>
  <files>evaluation/core/registry.py</files>
  <action>
    Create `evaluation/core/registry.py` with a simple `Registry` class that can register and retrieve classes by name. It should support datasets, runners, and metrics.
  </action>
  <verify>
    <automated>python -c "from evaluation.core.registry import Registry"</automated>
  </verify>
  <done>Registry class is created and importable</done>
</task>

</tasks>

<success_criteria>
- pyyaml and pydantic are successfully added as dependencies.
- A YAML configuration file can be parsed into strongly typed Pydantic models.
- The component registry allows registering and retrieving classes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-framework-architecture/06-01-SUMMARY.md`
</output>