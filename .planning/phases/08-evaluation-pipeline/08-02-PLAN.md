---
phase: 08-evaluation-pipeline
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [evaluation/runners/locomo.py, .planning/phases/08-evaluation-pipeline/templates/qa_prompt.txt]
autonomous: true
requirements: [PIPE-03, PIPE-04]
user_setup: []
must_haves:
  truths:
    - "LLM generates answers using Chain of Thought reasoning and retrieved MemBlocks context"
    - "QA prompt uses <context> XML tags to enclose retrieved context"
    - "Baseline comparison (raw history to LLM) is deferred per user decision"
  artifacts:
    - path: "evaluation/runners/locomo.py"
      provides: "LLM QA logic with CoT and prompt assembly"
      min_lines: 180
    - path: ".planning/phases/08-evaluation-pipeline/templates/qa_prompt.txt"
      provides: "QA prompt template with CoT instruction and context tags"
      contains: ["<context>", "Chain of Thought", "Answer the user's question"]
  key_links:
    - from: "evaluation/runners/locomo.py"
      to: "LLM client"
      via: "llm.generate() or equivalent API call"
      pattern: "llm\\.(generate|chat|complete)"
    - from: ".planning/phases/08-evaluation-pipeline/templates/qa_prompt.txt"
      to: "retrieved context"
      via: "<context> XML tags"
      pattern: "<context>.*</context>"
---

<objective>
Implement LLM-based question answering with Chain of Thought reasoning and handle baseline deferral for the evaluation pipeline.

Purpose: Fulfill PIPE-03 (LLM answering) with CoT and proper prompt structure, and address PIPE-04 by documenting the user's decision to defer baseline comparison.
Output: Updated LocomoRunner with QA logic, QA prompt template, and baseline deferral documentation.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/ROADMAP.md
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/REQUIREMENTS.md
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/phases/08-evaluation-pipeline/08-CONTEXT.md
@C:/Users/Lenovo/Desktop/MemBlocks/evaluation/runners/locomo.py
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/phases/08-evaluation-pipeline/08-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LLM QA with CoT (PIPE-03)</name>
  <files>evaluation/runners/locomo.py, .planning/phases/08-evaluation-pipeline/templates/qa_prompt.txt, tests/test_locomo_runner.py</files>
  <action>
    1. Create QA prompt template at templates/qa_prompt.txt with:
       - Standard Assistant persona: "Answer the user's question using the provided context."
       - Chain of Thought instruction: "Think step by step before providing your final answer."
       - Retrieved context in <context> XML tags: "<context>{retrieved_context}</context>"
       - Question: "Question: {question_text}"
       - Output format instruction: "Provide your final answer in 1-2 sentences."
    
    2. Update LocomoRunner._run_async() to implement LLM QA for each retrieval strategy:
       a. For each question, iterate over 3 retrieval strategies (semantic, core, hybrid)
       b. Load qa_prompt.txt template, fill in context and question
       c. Call LLM client (use RunnerConfig.model or default) with filled prompt
       d. Parse LLM response to extract final answer
       e. Store answer in eval_result: eval_result[f"answer_{strategy}"] = llm_answer
    
    3. Update tests/test_locomo_runner.py with test_qa() that mocks:
       - LLM client generate() method
       - Template loading
       - Verifies prompt contains <context> tags and CoT instruction
       - Verifies answer is stored per strategy
    
    Note: Use LLM client per Claude's discretion (model selection deferred to user). Prompt design follows user decisions: CoT, <context> tags, Standard Assistant persona.
  </action>
  <verify>
    <automated>pytest tests/test_locomo_runner.py::test_qa -x</automated>
  </verify>
  <done>
    - QA prompt template created with CoT, <context> tags, Standard Assistant persona
    - LLM generates answers for each of the 3 retrieval strategies per question
    - Prompt correctly injects retrieved context into <context> tags
    - Test coverage exists for QA logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Handle Baseline Deferral (PIPE-04)</name>
  <files>evaluation/runners/locomo.py, .planning/phases/08-evaluation-pipeline/08-02-PLAN.md</files>
  <action>
    Implement PIPE-04 deferral per user decision:
    1. In LocomoRunner._run_async(), add explicit comment block:
       """
       PIPE-04 Baseline Comparison: DEFERRED
       User Decision (08-CONTEXT.md): "Do not run baseline for now."
       Baseline would feed raw conversation history directly to LLM instead of MemBlocks context.
       This is skipped in initial implementation. To enable later, add a baseline strategy to retrieve_context().
       """
    2. Ensure no baseline code is present in the runner (remove any stub baseline code if exists)
    3. Update eval_result structure to explicitly note baseline is deferred:
       eval_result["baseline_status"] = "deferred_per_user_decision"
    
    Note: This addresses PIPE-04 requirement by documenting the modification per user decision, as required by CONTEXT.md.
  </action>
  <verify>
    <automated>pytest tests/test_locomo_runner.py::test_baseline_deferred -x</automated>
  </verify>
  <done>
    - PIPE-04 requirement is addressed via explicit deferral documentation
    - No active baseline comparison code exists in LocomoRunner
    - eval_result includes baseline deferral status
    - Test verifies baseline is marked as deferred
  </done>
</task>

</tasks>

<verification>
1. Run full test suite: pytest tests/test_locomo_runner.py -v
2. Verify qa_prompt.txt contains required elements: <context> tags, CoT instruction, Assistant persona
3. Confirm LocomoRunner has no active baseline code
4. Check eval_result structure includes answers for all 3 strategies plus baseline status
</verification>

<success_criteria>
1. PIPE-03 satisfied: LLM answers generated with CoT using retrieved MemBlocks context for all 3 strategies
2. PIPE-04 satisfied: Baseline comparison explicitly deferred with documentation per user decision
3. QA prompt follows all user-specified design decisions (CoT, <context> tags, Standard Assistant)
4. Test coverage for QA and baseline deferral exists and passes
5. No regressions in ingestion or retrieval logic from Plan 08-01
</success_criteria>

<output>
After completion, create `.planning/phases/08-evaluation-pipeline/08-02-SUMMARY.md`
</output>
