---
phase: 08-evaluation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [evaluation/runners/locomo.py, tests/test_locomo_runner.py]
autonomous: true
requirements: [PIPE-01, PIPE-02]
user_setup: []
must_haves:
  truths:
    - "Long-term conversation sessions are ingested into isolated MemBlocks blocks"
    - "Relevant context is retrieved from MemBlocks using 3 strategies (Semantic, Core, Hybrid)"
  artifacts:
    - path: "evaluation/runners/locomo.py"
      provides: "Ingestion and retrieval logic for LoCoMo sessions"
      min_lines: 120
    - path: "tests/test_locomo_runner.py"
      provides: "Test scaffold for runner functionality"
      min_lines: 50
  key_links:
    - from: "evaluation/runners/locomo.py"
      to: "memblocks_lib"
      via: "session.add() and session.flush() for ingestion"
      pattern: "session\\.(add|flush)"
    - from: "evaluation/runners/locomo.py"
      to: "MemBlocks retrieval API"
      via: "memblocks.retrieve() with strategy parameter"
      pattern: "memblocks\\.retrieve"
---

<objective>
Implement MemBlocks session ingestion and multi-strategy context retrieval for the LoCoMo evaluation pipeline.

Purpose: Fulfill PIPE-01 (ingestion) and PIPE-02 (retrieval) by building the core pipeline logic that processes LoCoMo sessions into MemBlocks and retrieves relevant context for QA.
Output: Updated LocomoRunner with working ingestion and retrieval, plus test coverage.
</objective>

<execution_context>
@C:/Users/Lenovo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/ROADMAP.md
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/REQUIREMENTS.md
@C:/Users/Lenovo/Desktop/MemBlocks/.planning/phases/08-evaluation-pipeline/08-CONTEXT.md
@C:/Users/Lenovo/Desktop/MemBlocks/evaluation/runners/locomo.py
@C:/Users/Lenovo/Desktop/MemBlocks/evaluation/runners/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MemBlocks Session Ingestion (PIPE-01)</name>
  <files>evaluation/runners/locomo.py, tests/test_locomo_runner.py</files>
  <action>
    Update LocomoRunner._run_async() to implement sequential session ingestion:
    1. For each LocomoSession in sessions:
       a. Create isolated MemBlocks block per session (using session.session_id as block identifier)
       b. Initialize MemBlocksClient with default config for each block
       c. Iterate through session.messages, call session.add(message) for each, then session.flush() after each message (per established memblocks_lib pattern)
    2. Add error handling for MemBlocks connection issues (try/except block)
    3. Create tests/test_locomo_runner.py with test_ingestion() that mocks MemBlocksClient and verifies:
       - One block created per session
       - session.add() called for each message
       - session.flush() called after each message
    
    Note: Use existing memblocks import pattern from locomo.py (try/except ImportError stub)
  </action>
  <verify>
    <automated>pytest tests/test_locomo_runner.py::test_ingestion -x</automated>
  </verify>
  <done>
    - Each LoCoMo session is ingested into a separate MemBlocks block
    - All messages in session are added via session.add() and flushed via session.flush()
    - Ingestion errors are caught and logged without crashing the pipeline
    - Test coverage exists for ingestion logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Multi-Strategy Context Retrieval (PIPE-02)</name>
  <files>evaluation/runners/locomo.py, tests/test_locomo_runner.py</files>
  <action>
    Add context retrieval logic to LocomoRunner._run_async() for each question in session.questions:
    1. Implement retrieve_context(question_text, block_client, strategy) method that calls:
       a. Semantic only: memblocks.retrieve(question_text, strategy="semantic", top_k=5)
       b. Core only: memblocks.retrieve(question_text, strategy="core", top_k=5)
       c. Hybrid: memblocks.retrieve(question_text, strategy="hybrid", top_k=5)
    2. Store retrieved context for each strategy in eval_result dict:
       - eval_result["retrieved_context_semantic"] = semantic_context
       - eval_result["retrieved_context_core"] = core_context
       - eval_result["retrieved_context_hybrid"] = hybrid_context
    3. Update tests/test_locomo_runner.py with test_retrieval() that mocks memblocks.retrieve() and verifies:
       - All 3 strategy calls are made for each question
       - Retrieved context is stored in eval_result
    
    Note: Use top_k=5 per Claude's discretion (can be tuned later). Strategies match user decision to run all three retrieval methods.
  </action>
  <verify>
    <automated>pytest tests/test_locomo_runner.py::test_retrieval -x</automated>
  </verify>
  <done>
    - All 3 retrieval strategies (semantic, core, hybrid) are implemented
    - For each question, context is retrieved using all 3 strategies
    - Retrieved context is stored per strategy in evaluation results
    - Test coverage exists for retrieval logic
  </done>
</task>

</tasks>

<verification>
1. Run full test suite: pytest tests/test_locomo_runner.py -v
2. Verify LocomoRunner has no stub comments remaining for ingestion/retrieval
3. Confirm all MemBlocks API calls match established memblocks_lib patterns (add/flush)
</verification>

<success_criteria>
1. PIPE-01 satisfied: LoCoMo sessions are ingested into isolated MemBlocks blocks sequentially
2. PIPE-02 satisfied: Context retrieved via 3 strategies (semantic, core, hybrid) for each question
3. Test coverage for ingestion and retrieval logic exists and passes
4. No regressions in existing LocomoDataset functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-evaluation-pipeline/08-01-SUMMARY.md`
</output>
